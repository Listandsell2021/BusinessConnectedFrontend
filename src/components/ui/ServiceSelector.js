import React from 'react';

// Service selector removed - only moving service supported
const ServiceSelector = () => {
  // Always return null as we only support moving service
  return null;
};

export default ServiceSelector;